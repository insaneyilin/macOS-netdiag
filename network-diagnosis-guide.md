# 网络诊断完全指南：从原理到实践

> 基于 2026-08-09 真实排障过程的完整复盘。
>
> **当你打不开网页且 AI 工具也无法使用时，这份文档 + netdiag.sh 就是你的备份大脑。**

---

## 目录

1. [复盘：3 小时排障时间线](#1-复盘3-小时排障时间线)
2. [原理：Wi-Fi 网络诊断必知必会](#2-原理wi-fi-网络诊断必知必会)
   - [L1 — Wi-Fi 物理层（802.11）](#l1--wi-fi-物理层80211)
   - [L2 — macOS 特有的 Wi-Fi 干扰源](#l2--macos-特有的-wi-fi-干扰源)
   - [L3 — 网络层](#l3--网络层)
   - [L4 — TCP 和应用层](#l4--tcp-和应用层)
3. [操作：系统化诊断流程](#3-操作系统化诊断流程)
4. [反思：断网时如何保持诊断能力](#4-反思断网时如何保持诊断能力)

---

## 1. 复盘：3 小时排障时间线

### 问题表现

- 手机和笔记本连同一个 Wi-Fi
- 手机上网流畅，看视频正常
- 笔记本打开百度都极慢，很多网页直接超时

### 诊断过程回顾

| 阶段 | 做了什么 | 发现 | 反思 |
|---|---|---|---|
| 1 | ping 百度、ping 网关 | 百度 50% 丢包，网关 100% 丢包（后来发现是路由器禁 ICMP） | 网关不响应 ping 不代表连不上，很多路由器禁 ICMP |
| 2 | 检查 DNS、代理、VPN | DNS 正常，无代理，Tailscale 已关闭 | DNS 解析快不代表网络好，应用层的问题可能在更底层 |
| 3 | 看 Wi-Fi 信号和信道 | 信号 -57 dBm（良好），但 MCS=0、TX Rate=6Mbps | **这是关键转折点**：SNR 正常但 MCS 极低是 Wi-Fi 层的 SOS 信号 |
| 4 | 怀疑 AWDL → 关闭 AWDL | 问题依旧，丢包从 94% 降到 55%，但仍严重 | AWDL 可能是加重因素但不是根因 |
| 5 | 怀疑蓝牙 → 关闭蓝牙 + 重启 Mac | 改善有限，丢包仍在 13-55% | 蓝牙天线分时也会影响，但也不是根因 |
| 6 | 用手机热点测试 | 热点连接流畅无比 | **决定性测试**：确定了 Mac 硬件没坏，问题出在路由器 |
| 7 | 重启路由器 | 路由器自动选了干净信道 (36)，MCS 恢复到 4，TX Rate 跳到 300+ Mbps | **根因确认**：路由器 5GHz 无线芯片卡在异常状态 |

### 关键教训

1. **手机热点是最强的诊断工具**：一次测试直接区分「Mac 问题」还是「路由器问题」
2. **MCS Index 是 Wi-Fi 的体温计**：降到 0 就是高烧，别在别处浪费时间
3. **路由器重启比任何本机操作都有效**：但需要先排除本机问题再操作
4. **诊断要分层**：从物理层一路查到应用层，不能跳跃

---

## 2. 原理：Wi-Fi 网络诊断必知必会

### L1 — Wi-Fi 物理层（802.11）

#### MCS Index：Wi-Fi 芯片的"体温计"

MCS（Modulation and Coding Scheme，调制编码方案）是 Wi-Fi 芯片根据信道质量**自动选择**的速率档位：

| MCS Index | 802.11ac 速率 (1 spatial stream, 80MHz) | 含义 |
|---|---|---|
| 8-9 | 390-433 Mbps | 信道质量极好，全速运行 |
| 6-7 | 263-351 Mbps | 正常 |
| 4-5 | 175-234 Mbps | 有轻微干扰，但可接受 |
| 2-3 | 78-117 Mbps | 干扰明显，网页可能卡顿 |
| 1 | 58 Mbps | 严重干扰，视频基本无法播放 |
| **0** | **29 Mbps** | **抢救模式：芯片认为信道几乎不可用** |

**关键洞察**：MCS 是芯片自行决定的，不是路由器分配的。MCS 降到 0 说明芯片在本地感知到了极强的干扰或极高的错误率，无论信号强度显示多好。

#### SNR（信噪比）vs MCS 的矛盾

正常情况下，SNR 和 MCS 正相关：信号越好，速率越高。但如果出现 **SNR ≥ 30dB 但 MCS ≤ 1**，这是一个明确的 SOS 信号——说明存在 SNR 测量**捕捉不到**的干扰：

- **非 Wi-Fi 干扰**：微波炉（2.4GHz）、雷达（5GHz DFS 频段）、某些 USB 3.0 设备
- **同频隐藏节点**：另一个 AP 在同一信道发送，你的 Mac 听不到它但路由器能听到，导致大量碰撞
- **天线分时**：蓝牙和 Wi-Fi 共用天线，蓝牙占用期间 Wi-Fi 完全无法收发
- **路由器无线芯片异常**：路由器进入某种故障状态，持续发送错误帧

#### 信道和频宽

```
5GHz 信道分布 (中国):
36──40──44──48  │  52──56──60──64  │  149──153──157──161──165
    (UNII-1)       (UNII-2/DFS)        (UNII-3)

频宽的影响:
20MHz → 占 1 个信道位
40MHz → 占 2 个连续信道位
80MHz → 占 4 个连续信道位  ← 更容易碰上邻居
160MHz → 占 8 个连续信道位 ← 极容易受干扰
```

**2.4GHz vs 5GHz 取舍**：

| | 2.4GHz | 5GHz |
|---|---|---|
| 穿墙能力 | 强 | 弱 |
| 最高速率 | 慢 | 快 |
| 可用信道数 | 3 个不重叠 (1/6/11) | 13+ 个不重叠 |
| 干扰源 | 微波炉、蓝牙、Zigbee、邻居 Wi-Fi | 雷达 (DFS)、邻居 Wi-Fi |
| 适合场景 | 穿墙多、设备多、追求稳定 | 近距离、追求速度 |

#### TX Rate（发射速率）

TX Rate 是当前时刻的实际发送速率，由 MCS + 空间流数 + 频宽 + Guard Interval 共同决定。它比 MCS 更直观地反映当前能跑多快。

**经验阈值**：
- `< 20 Mbps`：严重问题，网页都会卡
- `20-100 Mbps`：轻度问题，视频可能卡
- `> 200 Mbps`：正常

---

### L2 — macOS 特有的 Wi-Fi 干扰源

#### AWDL（Apple Wireless Direct Link）

AWDL 是 Apple 的专有协议，用于 AirDrop、AirPlay、Handoff 等功能的设备发现。它工作在你的 Wi-Fi 网卡所在的同一信道上，但**使用不同的 MAC 地址**（通过 `awdl0` 虚拟接口）。

**干扰机制**：AWDL 每隔约 60-300ms 发送一次探测帧。为了发送这些帧，Wi-Fi 芯片需要短暂切换到 AWDL 信道（如果与当前 Wi-Fi 信道不同）。每次切换 → 丢几个包 → TCP 触发重传 → 延迟累积。

**检测方法**：
```bash
ifconfig awdl0 | grep status    # active = 正在干扰
```

**修复方法**：
```bash
sudo ifconfig awdl0 down        # 立即生效，重启后自动恢复
```

#### 蓝牙共存（Bluetooth Coexistence）

Broadcom BCM4388 等芯片将 Wi-Fi 和蓝牙集成在同一芯片上，**物理上共用射频前端（天线）**。当蓝牙需要通信时（即使是待机状态的周期性扫描），Wi-Fi 必须等待。

**影响模式**：不是持续降速，而是**周期性尖峰**。ping 延迟在 2ms 和 500ms 之间剧烈抖动就是典型特征。

**检测**：
- 检查 `system_profiler SPBluetoothDataType` 中芯片型号是否为 BCM
- 检查蓝牙是否开启（即使没有连接设备，待机扫描也会干扰）

#### airportd 后台扫描

macOS 的 `airportd` 守护进程定期扫描附近 Wi-Fi 网络（用于定位服务、网络推荐等）。扫描时会短暂离开当前信道。

---

### L3 — 网络层

#### macOS DNS 解析路径

```
应用程序
  ↓
mDNSResponder (系统 DNS 解析器)
  ↓
scutil --dns 配置的 DNS 服务器
  ↓
实际可能被路由器劫持（DNS 重定向）
```

**如何确认 DNS 是否被路由器劫持**：如果 `scutil --dns` 显示 `8.8.8.8`，但 `nslookup` 显示 `192.168.18.1#53` 在响应，说明路由器在做 DNS 透明代理。

#### 双 NAT

```
你的设备 (192.168.18.x)
  → 路由器 (192.168.18.1) [NAT 第一次]
    → 光猫/上级路由 (192.168.1.1) [NAT 第二次]
      → ISP → 互联网
```

双 NAT 本身**不引起丢包**，但会导致：
- 端口转发失效
- UPnP 不工作
- 某些 P2P 应用连接困难

#### 路由器不响应 ping 是正常的

很多路由器出于安全考虑禁用了 ICMP 响应。所以 ping 网关 100% 丢包**不一定代表连不上**。更可靠的判断方法：
- 检查 ARP 表里有没有网关的 MAC 地址
- ping 公网 IP（如果能通，说明网关在正常工作只是不回应 ping）

---

### L4 — TCP 和应用层

#### 为什么 5% 丢包就让网页"很卡"

TCP 的设计哲学是「丢包 = 网络拥塞」。一旦检测到丢包（超时或收到 3 个重复 ACK），TCP 会：

1. **拥塞窗口 (cwnd) 减半**
2. **重传超时 (RTO) 翻倍**

实际影响：
```
0% 丢包 → 100 Mbps 吞吐 → 网页秒开
1% 丢包 → ~25 Mbps 吞吐 → 网页可接受
3% 丢包 → ~5 Mbps 吞吐 → 网页明显慢
5% 丢包 → ~2 Mbps 吞吐 → 网页转圈
10% 丢包 → <1 Mbps 吞吐 → 网页超时
```

#### 为什么 HTTPS 对丢包格外敏感

HTTPS（TLS 握手）需要多次往返：

```
客户端 → Server Hello
客户端 ← 服务器证书 (通常 3-5KB，需多个 TCP 段)
客户端 → 密钥交换
客户端 ← Finished
```

任何一次丢包都会触发 TCP 重传超时（最小 200ms，每次翻倍）。如果丢包是间歇性的，一次 TLS 握手叠加 2-3 次重传就是秒级延迟，超过浏览器默认超时就直接白屏。

**这就是今天看到的现象**：HTTP（无 TLS）29ms 正常，HTTPS 22 秒超时。

#### DNS over 丢包链路

```
DNS 查询 (UDP)
  ↓ 丢包
客户端等待超时 (1-5 秒)
  ↓
重试 DNS 查询
  ↓ 又丢包
再等 1-5 秒...
  ↓
用户感知: "网页白屏转圈 10 秒"
```

---

## 3. 操作：系统化诊断流程

### 决策树

```
网页打不开 / 极慢
│
├─ 是所有网站都慢，还是个别网站？
│  ├─ 个别网站慢 → DNS 或目标服务器问题
│  │  └─ 换个 DNS: nslookup 域名 8.8.8.8
│  │
│  └─ 所有网站都慢 → 继续 ↓
│
├─ ping 网关丢包 > 5%？
│  ├─ 是 → Wi-Fi 物理层问题
│  │  ├─ 关蓝牙
│  │  ├─ 关 AWDL: sudo ifconfig awdl0 down
│  │  ├─ 看 MCS Index (netdiag.sh L1)
│  │  └─ 重启路由器
│  │
│  └─ 否 → 继续 ↓
│
├─ ping 公网 (114.114.114.114) 丢包 > 10%？
│  ├─ 是 → 路由器 WAN/ISP 问题
│  │  └─ 重启光猫和路由器
│  │
│  └─ 否 → 继续 ↓
│
└─ ping 都正常但网页慢？
   ├─ 检查代理: scutil --proxy
   ├─ 检查 VPN: systemextensionsctl list
   └─ 检查 DNS 速度: nslookup www.baidu.com
```

### 一键诊断

```bash
bash netdiag.sh
```

30 秒内完成 6 层检查，输出诊断摘要 + 排序后的修复建议。

### 常用修复命令速查

| 操作 | 命令 |
|---|---|
| 关 AWDL | `sudo ifconfig awdl0 down` |
| 开 AWDL | `sudo ifconfig awdl0 up` |
| 关 Wi-Fi 再开 | `networksetup -setairportpower en0 off && sleep 2 && networksetup -setairportpower en0 on` |
| 续租 DHCP | `sudo ipconfig set en0 DHCP` |
| 刷新 DNS 缓存 | `sudo dscacheutil -flushcache && sudo killall -HUP mDNSResponder` |
| 删掉 Wi-Fi 重连 | 系统设置 → Wi-Fi → 详情 → 忽略此网络 → 重新连接 |
| 创建新网络位置 | 系统设置 → 网络 → 编辑位置 → + |

### 修复优先级

1. **本机软件**：关蓝牙 > 关 AWDL > 关 VPN/代理 > 刷新 DNS
2. **本机硬件层**：删掉 Wi-Fi 重连 > 创建新网络位置 > 重启 Mac
3. **路由器**：拔电源 30 秒重启 > 切换 2.4GHz > 重置路由器
4. **ISP**：联系运营商

---

## 4. 反思：断网时如何保持诊断能力

### 断网死循环

```
网络卡顿 → Cloud AI 不可用 → 无法诊断 → 网络继续卡顿
```

这是依赖 Cloud AI 的 coding agent 最大的单点故障。解决之道不是"有更好的 AI"，而是**在本地储备不依赖网络的诊断能力**。

### 三层备份策略

**第一层：知识（人脑）**
- 理解 MCS Index 的含义 → 一眼就能判断 Wi-Fi 芯片状态
- 记住决策树主干 → 不需要查文档也能走完前三步
- 原理比命令更重要：命令会忘，原理不会

**第二层：文档（本地文件）**
- `network-diagnosis-guide.md`：就是这份文档，放在本地，不需要网络就能打开
- 包含决策树 + 命令速查 + 原理讲解

**第三层：工具（脚本）**
- `netdiag.sh`：一键诊断，30 秒出结果
- 纯 bash + macOS 内置命令，零依赖，离线可用
- 输出自带中文解读，不需要 AI 翻译

### 最后的话

今天花了 3 小时解决的问题，如果一开始就用这套工具和决策树，大概 5 分钟就能定位到路由器：

1. `bash netdiag.sh` → MCS=0, SNR 正常 → Wi-Fi SOS
2. 关蓝牙 → 关 AWDL → 无效
3. 手机热点测试 → 流畅 → 问题在路由器
4. 重启路由器 → 解决

**核心原则：分层诊断，自底向上，一次只变一个变量。**
