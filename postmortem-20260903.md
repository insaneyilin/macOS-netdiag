# 故障复盘：2026-09-03 Wi-Fi 严重卡顿（第二次复发）

> 一句话结论：问题不在 Mac，而在二级路由器的 **5GHz 射频**——它「下行正常、上行失聪」，间歇性收不到 Mac 发出的帧，导致速率协商卡死在 MCS 0 / 6 Mbps。最终通过改直连上级路由器的 Wi-Fi 解决。
>
> 本次相比 08-09 第一次，最大的进步是：**没有停留在「路由器有问题」的黑盒结论，而是用 LQM 日志把根因钉到了「上行失聪」这个具体机制上。**

---

## 核心要点（若只记 5 条）

1. **根因不在 Mac，而在二级路由器的 5GHz 射频「上行失聪」**——下行干净（rxToss=0）、上行失败率 47–100%（txFail）、外部干扰为零（ccaOther=0）；同一病灶连续复发两次。

2. **诊断方法论升级**：LQM 日志计数器（txFail / rxToss / ccaOther）把结论从 08-09 的黑盒「路由器有问题」推进到机制级「上行失聪」——更精确，也直接指向「换设备 / 换频段」的处置。

3. **流程纪律反复失效（批判）**：决定性测试「手机热点」连续两次漏做；且当 `ping IP` 已 100% 丢包时仍在改 DNS——工具箱有了，决策树没被真正执行。

4. **工具存在误导性缺陷**：`114.114.114.114` 不响应 ICMP，持续制造「公网 100% 丢包」假警报，两次干扰判断（已改为 `223.5.5.5`）。

5. **两个隐蔽陷阱**：双频同 SSID + macOS 频段引导，解释了「手机流畅 Mac 卡」（强但失聪的 5GHz 压过健康的 2.4GHz）；而 SSID 本身泄露房间号——复盘产物必须脱敏后才可外传。

---

## 1. 故障现象

- 手机和 Mac 连同一个 Wi-Fi（SSID 已隐去）
- 手机看视频、刷网页流畅
- Mac 基本无法上网：网页白屏/转圈，很多站点直接超时

## 2. 时间线

| 时间 | 事件 | 关键数据 |
|---|---|---|
| 09-04 00:59 | 第 1 次 `netdiag.sh` | MCS 0 / TX 6Mbps / SNR 35dB，网关丢包 **76%**，HTTPS 超时 |
| 09-04 01:03 | 关闭蓝牙后 | MCS 1，**网关/DNS 丢失**（路由表异常） |
| 09-04 01:07 | `sudo ifconfig awdl0 down` 后 | MCS 0，网关仍丢失，AWDL 仍报「已激活」 |
| 09-04 01:12 | 手动改 DNS 为 114.114.114.114 | MCS 0，网关恢复，丢包 **92%** |
| 09-04 01:18 | Mac 漫游到 **2.4GHz**（信道 8） | （来自系统日志） |
| 09-04 01:22 | Mac 又切回 **5GHz**（信道 149/80） | （来自系统日志） |
| 09-04 01:23 | 第 5 次 `netdiag.sh` | MCS 0，丢包 20%，HTTPS 勉强通过（4.2s） |
| 09-04 01:31–01:32 | macOS LQM 日志抓取 | 上行失败率 **47%–100%**，下行 0 丢弃（见 §4） |
| 09-04 21:58 | 次日复查 | MCS 4 / TX 117Mbps / 0% 丢包，已恢复 |
| 09-05 | **改直连智慧沃家路由器 Wi-Fi** | 网速明显好转，问题解决 |

## 3. 排查过程回顾

### 已经正确排除的（有据可依）

| 尝试 | 结果 | 结论 |
|---|---|---|
| 关蓝牙 | MCS 仍为 0 | 排除（第二次运行起关闭，无效） |
| 关 AWDL | MCS 仍为 0 | 排除（注意：关闭后脚本仍报「已激活」，见 §7） |
| 改 DNS（114 / 8.8.8.8） | 无效 | **白费功夫**——问题在 IP 层之下，`ping IP` 已 100% 丢包，与 DNS 无关 |
| 检查代理/VPN | 一直无代理无 VPN | 排除 |

### 误判点：`ping 114.114.114.114` 的假警报

诊断脚本一直报「公网严重丢包 114 DNS 丢包 100%」。实测证明 **114.114.114.114 根本不响应 ICMP**（对比 `223.5.5.5` 0% 丢包、`8.8.8.8` 0% 丢包）。这个误报在两次故障中都存在，且干扰了判断——真正的断网信号是「网关丢包 + HTTPS 超时」，而不是对 114 的 ping。

> 已修复：脚本 L5 的探测目标改为 `223.5.5.5`。

### 漏做的关键一步（连续第二次）

**手机热点测试**没有做。手册里明确定义它是「决定性测试」，但 5 次运行中一次都没执行。事后看，最终「改直连智慧沃家」恰好等价于这个测试——换了个健康路由器，Mac 立刻正常，从而反证 Mac 硬件没问题。

## 4. 决定性证据：LQM 日志

`system_profiler` 的 MCS/速率只是瞬时采样，不足以定位机制。真正的定案证据来自 macOS 统一日志里 Wi-Fi 芯片的 LQM（Link Quality Management）计数器：

| 时间 | txFrames | txFail | 上行失败率 | txFwRetrans | rxFrames | rxToss | ccaOther（外部干扰） |
|---|---|---|---|---|---|---|---|
| 01:31:18 | 186 | 87 | 47% | 2730 | 8682 | 0 | 0 |
| 01:31:23 | 119 | 94 | 79% | 5330 | 350 | 0 | 0 |
| 01:31:33 | 161 | 161 | **100%** | 11180 | 210 | 0 | 0 |
| 01:31:43 | 220 | 204 | 93% | 6890 | 487 | 0 | 0 |
| 01:31:48 | 206 | 42 | 20% | 3120 | 1330 | 0 | 0 |
| 01:32:13 | 77 | **0** | **0%** | 8 | 367 | 0 | 0 |

字段含义：`txFrames` 发送帧数、`txFail` 未收到 ACK 的失败帧、`txFwRetrans` 固件级重传次数、`rxToss` 接收丢弃帧数、`ccaOther` 来自其他网络的信道占用。

### 从这组数字能读出的四条铁证

1. **下行完美**：`rxToss=0`、`rxRetryFrames=0`，接收数千帧一个不丢 → Mac 的接收链路、天线、射频前端完全健康。
2. **上行崩坏**：`txFail` 高达 47%–100%，`txFwRetrans` 冲到上万次 → Mac 在疯狂重发同一帧，因为**路由器一直不回 ACK**。
3. **零外部干扰**：`ccaOtherTotal=0`、`interferenceTotal=1~3` → 直接排除邻居竞争/隐藏节点/微波炉。
4. **硬件「能好、只是会卡」**：01:31:48 速率瞬间恢复 40.5 Mbps、失败率降到 20%，01:32:13 甚至 `txFail=0` → 若是发射功放硬损坏不会这样「闪恢复」。

### 根因结论

> **那台二级路由器的 5GHz 射频在接收（上行）方向间歇性失聪**：能正常发（Mac 听得清楚），但收不到/不确认 Mac 发出的帧，导致 Mac 的速率协商一路跌到 MCS 0 并卡死。

## 5. 「手机流畅」之谜：双频同 SSID + 频段引导陷阱

系统日志的扫描结果暴露了关键事实：路由器把同一个 SSID（已隐去）挂在两个独立射频上：

```
bssList = [ (信道 5g149/80, 关联于 01:22:32),   ← Mac 连这个（坏的 5GHz）
            (信道 2g8/20,   关联于 01:18:17) ]   ← 手机大概率连这个（好的 2.4GHz）
```

- Mac 卡在坏掉的 5GHz 射频；手机连的是健康的 2.4GHz → 所以「手机流畅、Mac 卡」。
- macOS 自己也中招：01:18:17 曾漫游到 2.4GHz，01:22:32 又跳回 5GHz——因为 5GHz 信号很强（-55dBm），频段引导逻辑误判「信号强=更好」，结果切回了那个「信号强但已失聪」的坏射频。

## 6. 本次 vs 08-09 第一次对比

| 维度 | 08-09（第一次） | 09-03（本次） |
|---|---|---|
| 根因 | 路由器 5GHz 芯片卡死 | 路由器 5GHz 上行失聪（更精确） |
| 定位手段 | 手机热点（黑盒排除法） | **LQM 日志（机制级定位）** |
| 结论精度 | 「路由器有问题」 | 「路由器 5GHz 上行失聪」 |
| 最终解决 | 重启路由器（自动换信道） | 改直连上级路由器（绕开坏设备） |
| 共性失误 | 未做热点测试 | 未做热点测试（第二次）+ 被 114 误报误导 |

**核心进步**：这次没有停留在黑盒结论，而是用日志把根因推进到了具体机制。这是诊断能力的一次实质性升级——「上行失聪」这个结论直接指向「换设备/换频段」的处置，而非含糊的「重启试试」。

## 7. 做得不足的地方（流程层面）

1. **热点测试连续第二次漏做**。它是区分「Mac 问题 vs 路由器问题」的最快手段，却两次都被跳过。
2. **在 DNS 上浪费了时间**。当 `ping IP` 已经 100% 丢包时，改 DNS 注定无效——问题在 IP 层之下。决策树里有这条，但现场没守住。
3. **AWDL 关闭后脚本仍报「已激活」**。可能是 down 后系统又拉起，也可能脚本检测逻辑有误——需核实，否则会误导判断。
4. **`114.114.114.114` 误报**导致 L5 层「公网 100% 丢包」的假信号干扰了判断（已修复）。

## 8. 行动项 / 改进清单

- [x] 修复 `netdiag.sh` L5 的 ICMP 探测目标（114 → 223.5.5.5）
- [x] 记录家宽拓扑变化（改直连智慧沃家路由器）
- [ ] 把 LQM 日志解析（`txFail / rxToss / ccaOther`）加进 `netdiag.sh`，让脚本下次直接输出「上行失聪 vs 下行干扰」
- [ ] 核实 AWDL 关闭检测逻辑
- [ ] 处置那台二级路由器：退役，或将其 5G/2.4G SSID 分离只连 2.4G
- [ ] 若需保留那台二级路由器补信号，考虑拆分双频 SSID，避免 macOS 频段引导再次误切

## 9. 附录：LQM 关键字段速查

| 字段 | 含义 | 健康值 |
|---|---|---|
| `txFrames` / `txFail` | 发送帧数 / 未 ACK 的失败帧 | txFail ≈ 0 |
| `txFwRetrans` | 固件级重传次数 | 接近 0（数千=灾难） |
| `rxToss` | 接收丢弃帧数 | 0 |
| `ccaOther` | 来自其他网络的信道占用 | 低 |
| `per_ant_rssi` | 每根天线 RSSI | 两根均衡 |

抓取命令：

```bash
log show --start "<起始时间>" --end "<结束时间>" \
  --style compact --predicate 'process == "airportd"' \
  2>/dev/null | grep -E "corewifi.*LQM"
```

---

*诊断工具的完整原理见 `network-diagnosis-guide.md`，一键诊断脚本见 `netdiag.sh`。*
